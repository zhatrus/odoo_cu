# GPS Fleet Management - Інструкція з Встановлення та Презентації

## 📦 Частина 1: Встановлення Модуля (з нуля)

### Крок 1: Клонування Репозиторію

```bash
# Перейти до директорії з custom addons
cd /home/mini/odoo/odoo19/custom_addons

# Клонувати репозиторій
git clone https://github.com/zhatrus/odoo_cu.git

# Перейти до директорії модуля
cd odoo_cu/gps_route_sheet
```

### Крок 2: Оновлення Списку Модулів в Odoo

```bash
# Перезапустити Odoo сервер
odoo-helper server stop
odoo-helper server start

# Або оновити список додатків через UI
# Settings > Apps > Update Apps List
```

### Крок 3: Встановлення Модуля

**⚠️ ВАЖЛИВО: Для демонстрації встановлюйте модуль з demo data!**

**Варіант А: Через UI (з demo data)**
1. Відкрити браузер: `http://localhost:15069/`
2. Увійти як адміністратор
3. Перейти: Settings > Activate the developer mode
4. Перейти: Apps > Remove "Apps" filter
5. Search "GPS Fleet Management"
6. Натиснути "Install"
7. ✅ Demo data встановиться автоматично

**Варіант Б: Через командний рядок (з demo data)**
```bash
# Встановити модуль з demo data
odoo-helper addons install gps_route_sheet

# Або через odoo-bin з явним включенням demo
/home/mini/odoo/odoo19/venv/bin/python3 /home/mini/odoo/odoo19/odoo/odoo-bin \
  -c /home/mini/odoo/odoo19/conf/odoo.conf \
  -d odoo19 -i gps_route_sheet --without-demo=False --stop-after-init
```

**Що включає Demo Data:**
- ✅ 3 тестові транспортні засоби (Ford Focus, Toyota Camry, VW Passat)
- ✅ 3 маршрутні листи з різними статусами
- ✅ 10+ тестових поїздок з реальними маршрутами
- ✅ Цілі поїздок (Ділова поїздка, Доставка, Сервіс, Зустріч)
- ✅ GPS дані (IMEI, паливні картки, норми витрат)

**Якщо модуль вже встановлено БЕЗ demo data:**
```bash
# Видалити модуль
odoo-helper addons uninstall gps_route_sheet

# Встановити заново з demo data
odoo-helper addons install gps_route_sheet
```

### Крок 4: Перевірка Встановлення

```bash
# Перевірити що модуль встановлено без помилок
odoo-helper server log

# Запустити тести
odoo-helper test gps_route_sheet

# Перевірити якість коду
odoo-helper lint flake8 gps_route_sheet
odoo-helper lint pylint gps_route_sheet
```

---

## 🔧 Частина 2: Налаштування через UI

### 2.1. Базова Конфігурація

1. **Налаштування підключення до GPS БД**
   - Перейти: `Settings > GPS Route Sheet Settings`
   - Заповнити параметри підключення:
     - Host: `localhost` (або IP GPS сервера)
     - Port: `5432`
     - Database: `gps_db`
     - User: `gps_user`
     - Password: `***`
   - Натиснути "Save"
   - Натиснути "Test Connection" для перевірки

2. **Синхронізація Довідників**
   - Перейти: `GPS Route Sheets > Sync Vehicles from GPS`
   - Вибрати режим: "Create new and update existing"
   - Натиснути "Sync Vehicles"
   
   - Перейти: `GPS Route Sheets > Sync Trip Purposes from GPS`
   - Натиснути "Sync Trip Purposes"

3. **Налаштування Користувачів та Прав Доступу**
   - Перейти: `Settings > Users & Companies > Users`
   - Створити/редагувати користувача
   - Додати до групи:
     - `GPS Route Sheet / User` - для звичайних користувачів
     - `GPS Route Sheet / Manager` - для менеджерів

### 2.2. Робота з Модулем

1. **Перегляд GPS Dashboard**
   - Перейти: `GPS Route Sheets > GPS Dashboard`
   - Переглянути картки транспортних засобів з live GPS даними
   - Натиснути на картку для детальної інформації

2. **GPS Tracking (Карта)**
   - Перейти: `GPS Route Sheets > GPS Tracking`
   - Переглянути всі ТЗ на карті з поточними позиціями

3. **Створення Маршрутного Листа**
   - Перейти: `GPS Route Sheets > Route Sheets`
   - Натиснути "Create"
   - Вибрати транспортний засіб
   - Вказати діапазон дат
   - Натиснути "Import Trips from GPS"
   - Переглянути та відредагувати поїздки
   - Натиснути "Print" для друку

4. **Аналітика**
   - Перейти: `GPS Route Sheets > Route Sheets`
   - Натиснути на іконку графіка для Pivot View
   - Аналізувати дані по різним параметрам

---

## 🎤 Частина 3: План Презентації Модуля

### 3.1. Вступ (2 хв)

**Що презентувати:**
- Назва модуля: **GPS Fleet Management**
- Призначення: Автоматизація обліку поїздок транспортних засобів з GPS трекерами
- Основна проблема: Ручне ведення маршрутних листів, відсутність контролю за пальним

**Де показати в коді:**
- `__manifest__.py` - метадані модуля, залежності
- `README.rst` - повний опис функціоналу

**Де показати в UI:**
- Apps > GPS Fleet Management - опис модуля
- `static/description/index.html` - красива презентація

---

### 3.2. Демонстрація Вимог Курсової Роботи

#### ✅ 1. Моделі (мінімум 3)

**Код:**
```
models/
├── gps_route_sheet.py      # GpsRouteSheet, GpsRouteTrip, GpsTripPurpose
├── fleet_vehicle.py         # FleetVehicle (extends)
├── gps_db.py               # GpsDbService
└── res_config_settings.py  # ResConfigSettings (extends)
```

**UI:**
- `GPS Route Sheets > Route Sheets` - модель GpsRouteSheet
- `GPS Route Sheets > Route Trips` - модель GpsRouteTrip
- `GPS Route Sheets > Trip Purposes` - модель GpsTripPurpose

**Показати:**
- Різні типи полів (Char, Integer, Float, Date, Many2one, One2many)
- Computed fields (`current_latitude`, `current_longitude`)
- Related fields, constraints

---

#### ✅ 2. Views (List, Form, Kanban, Pivot, Graph)

**Код:**
```
views/
├── gps_route_sheet_views.xml    # list, form, pivot, graph
├── gps_dashboard.xml            # kanban view для GPS Dashboard
├── gps_tracking_map.xml         # kanban з картою
└── fleet_vehicle_views.xml      # extended form view
```

**UI:**
- **List View**: `GPS Route Sheets > Route Sheets` (список)
- **Form View**: Відкрити будь-який маршрутний лист
- **Kanban View**: `GPS Route Sheets > GPS Dashboard`
- **Pivot View**: `GPS Route Sheets > Route Sheets` > Graph icon > Pivot
- **Graph View**: `GPS Route Sheets > Route Sheets` > Graph icon > Graph
- **Tree View**: Trips всередині Route Sheet (One2many)

---

#### ✅ 3. Wizard (мінімум 1)

**Код:**
```
wizard/
├── vehicle_sync_wizard.py              # Синхронізація ТЗ
├── trip_purpose_sync_wizard.py         # Синхронізація цілей поїздок
└── route_trip_import_wizard.py         # Імпорт поїздок
```

**UI:**
- `GPS Route Sheets > Sync Vehicles from GPS` - wizard з вибором режиму
- Всередині Route Sheet: кнопка "Import Trips" - wizard імпорту

**Показати:**
- TransientModel
- Різні режими роботи (create_new, update_existing, create_and_update)
- Обробка даних та повернення результату

---

#### ✅ 4. Scheduled Actions (Cron)

**Код:**
```xml
<!-- data/ir_cron.xml -->
<record id="ir_cron_sync_trips" model="ir.cron">
    <field name="name">Sync GPS Trips</field>
    <field name="interval_number">5</field>
    <field name="interval_type">minutes</field>
</record>
```

**UI:**
- `Settings > Technical > Automation > Scheduled Actions`
- Знайти: "Sync GPS Trips" та "Sync Fuel Transactions"

**Показати:**
- Автоматична синхронізація кожні 5 хвилин (trips)
- Автоматична синхронізація кожну годину (fuel)
- Можливість запустити вручну

---

#### ✅ 5. Server Actions

**Код:**
```xml
<!-- data/ir_actions_server.xml -->
<record id="action_sync_all_odometers" model="ir.actions.server">
    <field name="name">Sync All Odometers</field>
    <field name="model_id" ref="fleet.model_fleet_vehicle"/>
    <field name="binding_model_id" ref="fleet.model_fleet_vehicle"/>
    <field name="state">code</field>
</record>
```

**UI:**
- `Fleet > Vehicles` > Вибрати кілька ТЗ
- `Action > Sync All Odometers`

**Показати:**
- Масова операція на вибраних записах
- Виконання Python коду на сервері

---

#### ✅ 6. Reports (QWeb)

**Код:**
```
report/
└── gps_route_sheet_report.xml    # QWeb template для друку
```

**UI:**
- Відкрити будь-який Route Sheet
- Натиснути "Print" > "Route Sheet Report"

**Показати:**
- Професійний друкований звіт
- Таблиці з поїздками
- Підсумкові дані (загальний пробіг, паливо)

---

#### ✅ 7. Зовнішня Інтеграція (PostgreSQL)

**Код:**
```python
# models/gps_db.py
class GpsDbService(models.AbstractModel):
    def fetch_trips(self, imei, date_from, date_to):
        # Підключення до зовнішньої PostgreSQL БД
        with psycopg2.connect(**params) as conn:
            ...
```

**UI:**
- `Settings > GPS Route Sheet Settings` - налаштування підключення
- Кнопка "Test Connection" - перевірка з'єднання
- Результат синхронізації даних з GPS БД

**Показати:**
- Підключення до зовнішньої БД
- Отримання даних (trips, positions, fuel transactions)
- Обробка помилок

---

#### ✅ 8. Demo Data

**Код:**
```
demo/
└── demo_data.xml    # Демонстраційні дані
```

**UI:**
- Встановити модуль з demo data
- Переглянути готові Route Sheets з даними

**Показати:**
- Автоматичне створення тестових даних
- Готові приклади для демонстрації

---

#### ✅ 9. Security (Groups, Access Rights, Record Rules)

**Код:**
```
security/
├── security.xml           # Групи користувачів
└── ir.model.access.csv    # Права доступу до моделей
```

**Групи користувачів:**
- `GPS Route Sheet / User` - базові права
- `GPS Route Sheet / Manager` - повні права

**UI:**
- `Settings > Users & Companies > Groups`
- Знайти групи "GPS Route Sheet"

**Record Rules:**
```xml
<!-- User може бачити тільки свої Route Sheets -->
<record id="gps_route_sheet_user_rule" model="ir.rule">
    <field name="domain_force">[('create_uid','=',user.id)]</field>
</record>
```

**Показати:**
- Увійти як User - бачить тільки свої записи
- Увійти як Manager - бачить всі записи
- Різні права на CRUD операції

---

#### ✅ 10. Документація

**Код:**
```
├── README.rst                      # Повна документація
├── static/description/
│   ├── index.html                  # HTML опис з картинками
│   ├── banner.png                  # Банер модуля
│   └── icon.png                    # Іконка модуля
└── models/*.py                     # Docstrings у всіх класах/методах
```

**UI:**
- Apps > GPS Fleet Management - переглянути опис з `index.html`

**Показати:**
- README.rst з детальним описом
- Docstrings у коді
- Коментарі для складної логіки
- HTML презентація з картинками

---

#### ✅ 11. Переклади (Українська мова)

**Код:**
```
i18n/
└── uk.po    # Український переклад (500+ рядків)
```

**UI:**
- Змінити мову інтерфейсу на українську
- Переглянути модуль - всі написи українською

**Показати:**
- Перемкнути мову в Settings > Translations
- Всі меню, поля, кнопки українською

---

#### ✅ 12. Якість Коду

**Команди перевірки:**
```bash
# Flake8 - перевірка стилю коду
odoo-helper lint flake8 gps_route_sheet
# Результат: 0 помилок

# Pylint - статичний аналіз
odoo-helper lint pylint gps_route_sheet
# Результат: 10.00/10

# Unit тести
odoo-helper test --coverage gps_route_sheet
# Результат: 22/22 тести пройдено
```

**Показати:**
- Виконати команди в терміналі
- Показати результати без помилок
- Показати coverage report

---

### 3.3. Додаткові Фічі (Бонуси)

#### 🎯 GPS Live Dashboard
- Real-time дані з GPS трекерів
- Kanban view з кольоровими картками
- Статус запалювання (ON/OFF)
- Поточна швидкість, координати, рівень палива

#### 🗺️ GPS Tracking Map
- Інтерактивна карта з позиціями всіх ТЗ
- Leaflet.js інтеграція
- Клік на маркер - детальна інформація

#### 📊 Advanced Analytics
- Pivot tables для аналізу поїздок
- Графіки споживання палива
- Порівняння з нормами
- Виявлення перевищень швидкості

#### 🔄 Auto-sync
- Автоматична синхронізація кожні 5 хвилин
- Фонові cron jobs
- Логування помилок

#### 🧪 Unit Tests
- 22 unit тести
- Coverage для всіх основних компонентів
- Mock зовнішніх залежностей

---

### 3.4. Структура Презентації (15-20 хв)

1. **Вступ** (2 хв)
   - Проблема та рішення
   - Основний функціонал

2. **Архітектура** (3 хв)
   - Моделі та їх зв'язки
   - Інтеграція з Fleet модулем
   - Підключення до GPS БД

3. **Демонстрація UI** (5 хв)
   - GPS Dashboard
   - Створення Route Sheet
   - Імпорт поїздок
   - Друк звіту

4. **Технічні Аспекти** (5 хв)
   - Wizards
   - Cron jobs
   - Security (groups, rules)
   - QWeb reports

5. **Якість та Тестування** (3 хв)
   - Lint результати
   - Unit тести
   - Документація

6. **Питання та Відповіді** (2 хв)

---

## 📋 Чеклист Перед Презентацією

### Перевірити Встановлення:
- [ ] Модуль встановлено без помилок
- [ ] Всі залежності встановлені
- [ ] Demo data завантажено

### Перевірити Функціонал:
- [ ] GPS Dashboard відображається
- [ ] Можна створити Route Sheet
- [ ] Імпорт поїздок працює
- [ ] Звіт друкується
- [ ] Cron jobs активні

### Перевірити Якість:
- [ ] `odoo-helper lint flake8` - 0 помилок
- [ ] `odoo-helper lint pylint` - 10/10
- [ ] `odoo-helper test` - всі тести пройдено
- [ ] Модуль встановлюється/видаляється без помилок

### Підготувати Демо:
- [ ] Створити тестові Route Sheets
- [ ] Підготувати дані для демонстрації
- [ ] Перевірити всі UI екрани
- [ ] Підготувати відповіді на можливі питання

---

## 🚀 Швидкий Старт для Презентації

```bash
# 1. Клонувати репозиторій
cd /home/mini/odoo/odoo19/custom_addons
git clone https://github.com/zhatrus/odoo_cu.git

# 2. Встановити модуль
odoo-helper addons install gps_route_sheet

# 3. Перевірити якість
odoo-helper lint flake8 gps_route_sheet
odoo-helper lint pylint gps_route_sheet
odoo-helper test gps_route_sheet

# 4. Відкрити в браузері
# http://localhost:15069/
# Apps > GPS Fleet Management
```

---

## 📞 Корисні Команди

```bash
# Перезапустити сервер
odoo-helper server restart

# Оновити модуль після змін
odoo-helper addons update gps_route_sheet

# Переглянути логи
odoo-helper server log

# Видалити модуль
odoo-helper addons uninstall gps_route_sheet

# Створити backup БД
odoo-helper db backup odoo19

# Відновити backup
odoo-helper db restore odoo19 backup_file.zip
```

---

**Успішної презентації! 🎉**
